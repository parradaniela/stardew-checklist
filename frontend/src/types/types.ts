export type ForageObj = {
    name: string;
    imgUrl: string;
    wikiUrl: string;
    locations: string[];
};

export type radioAttributeType = {
    label: string;
    name: string;
    id: string;
    value: string;
};
